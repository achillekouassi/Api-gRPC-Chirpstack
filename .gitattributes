/mvnw text eol=lf
*.cmd text eol=crlf

# Forcer LF (Unix-style) pour tous les fichiers texte
* text=auto eol=lf

# Les fichiers .proto doivent toujours être en LF (très important)
*.proto text eol=lf

# Facultatif : fichiers Windows-only (si jamais)
*.bat text eol=crlf
*.ps1 text eol=crlf

